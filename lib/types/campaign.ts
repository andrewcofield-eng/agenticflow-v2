export type CampaignBrief = {
  goal: string;
  segment: string;
  productFocus: string;
  theme: string;
};

export type CampaignPackage = {
  name: string;
  objective: string;
  targetAudience: string;
  rationale: string[];
};
